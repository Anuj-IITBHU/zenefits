#include <bits/stdc++.h>
#include <string>
using namespace std;

inorder (string s, int i)
{
    if(s[i])
}

int main() {
	int t;
	cin >> t;
	string s[t];
	for(int i=0;i<t;i++)
	{
	    cin >> s[i];
	}
	for(int i=0;i<t;i++)
	    inorder(s[i], 0);
    return 0;
}
